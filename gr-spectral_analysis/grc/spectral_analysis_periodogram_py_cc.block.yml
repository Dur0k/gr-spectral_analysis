# auto-generated by grc.converter

id: spectral_analysis_periodogram_py_cc
label: Periodogram
category: '[spectral_analysis]'

parameters:
-   id: sample_rate
    label: Sampling Rate
    dtype: float
-   id: fft_size
    label: FFT Size
    dtype: int
    default: '1'
    hide: ${ 'part' if fft_size == 1 else 'none' }
-   id: window
    label: Window(scipy)
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fft_size }

outputs:
-   label: out0
    domain: stream
    dtype: float
    vlen: ${ fft_size }
-   label: out1
    domain: stream
    dtype: complex
    vlen: ${ fft_size }

templates:
    imports: import spectral_analysis
    make: spectral_analysis.periodogram_py_cc(${sample_rate}, ${fft_size}, ${window})

file_format: 1
