# auto-generated by grc.converter

id: spectral_analysis_peak_finding_cf
label: peak_finding_cf
category: '[spectral_analysis]'

parameters:
-   id: fft_size
    label: FFT Size
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: sensor_count
    label: Sensor Count
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: thres
    label: Peak Threshold
    dtype: float
-   id: min_dist
    label: Peak Distance
    dtype: float

inputs:
-   label: in0
    domain: stream
    dtype: float
    vlen: ${ fft_size }
-   label: in1
    domain: stream
    dtype: complex
    vlen: ${ fft_size }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ sensor_count }

templates:
    imports: import spectral_analysis
    make: spectral_analysis.peak_finding_cf(${fft_size}, ${sensor_count}, ${thres},
        ${min_dist})

file_format: 1
